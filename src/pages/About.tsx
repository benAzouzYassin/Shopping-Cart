import React from 'react'
import Navbar from '../components/Navbar'
import data from "../data/items.json"

export default function About() {
    const imgs = data.map(item => item.imgUrl)

    return (
        <div><Navbar />
            a


            a
        </div>
    )
}
